
from django.contrib import admin
from django.urls import path
from . import views

urlpatterns = [
    # path('api/courses/<int:course_id>/timetable/', views.CourseTimetableView.as_view(), name='course-timetable'),
    # path('api/timetable/add/', views.AddTimetableView.as_view(), name='add-timetable')
    
    

    
]
